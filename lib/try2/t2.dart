import 'package:flutter/material.dart';

class Try2 extends StatelessWidget {
  const Try2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("This is try2."),
    );
  }
}
